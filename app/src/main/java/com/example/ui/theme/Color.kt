package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// 1. Brand Accents: Antique Gold & Oxford Blue (High Contrast)
// ==========================================
// Illuminated Manuscript Gold (High contrast Amber Bronze for Light mode, Luminous for Dark mode)
val GoldPrimary = Color(0xFFB45309)          // Rich Amber Bronze (AAA contrast on light surfaces)
val GoldDark = Color(0xFF78350F)             // Deep Bronze Ink
val GoldLight = Color(0xFFFBBF24)            // Luminous Warm Gold for dark mode
val GoldContainer = Color(0xFFFEF3C7)        // Soft warm highlight container
val OnGoldContainer = Color(0xFF78350F)      // Dark amber ink on gold container

// Scholarly Oxford Blue (Authoritative academic actions, active tools, selection)
val ScholarBlue = Color(0xFF1E3A8A)          // Deep Navy / Oxford Blue
val ScholarBlueDark = Color(0xFF0F172A)      // Midnight Navy Slate
val ScholarBlueLight = Color(0xFF60A5FA)     // Crisp Sky Blue for dark mode
val ScholarBlueContainer = Color(0xFFDBEAFE) // Soft blue highlight container
val OnScholarBlueContainer = Color(0xFF1E3A8A)
val ScholarBlueAccent = Color(0xFF2563EB)

// ==========================================
// 2. Crisp Paper & Archival Canvas (Light Mode)
// ==========================================
val ParchmentBg = Color(0xFFF1F5F9)          // Clean modern light desk canvas (Slate 100)
val ParchmentSurface = Color(0xFFFFFFFF)     // Pure Clean White Paper
val ParchmentSurfaceVariant = Color(0xFFF8FAFC) // Crisp secondary panels / toolbars
val ParchmentBorder = Color(0xFFCBD5E1)      // Crisp structural border
val ParchmentText = Color(0xFF0F172A)        // Deep Carbon Slate Black Ink
val ParchmentMuted = Color(0xFF475569)       // Highly readable Slate Gray (WCAG AAA)

// ==========================================
// 3. Deep Scriptorium Codex (Dark Mode)
// ==========================================
val DarkBg = Color(0xFF0B1120)               // Midnight codex canvas
val DarkSurface = Color(0xFF131D2E)          // Primary dark panel surface
val DarkSurfaceVariant = Color(0xFF1E293B)   // Elevated cards, toolbars, chips
val DarkBorder = Color(0xFF334155)           // Restrained slate border
val DarkText = Color(0xFFF8FAFC)             // High-legibility parchment silver
val DarkMuted = Color(0xFF94A3B8)            // Subdued guide & secondary labels

// ==========================================
// 4. Typology Palette (Philological Taxonomy)
// ==========================================
val TypoMatan = Color(0xFF0284C7)            // Sapphire Blue for Matan (Core text)
val TypoSyarah = Color(0xFFD97706)           // Warm Amber Ochre for Syarah (Commentary)
val TypoMarginalia = Color(0xFF7C3AED)       // Amethyst Violet for Marginalia / Hasyiyah
val TypoFootnote = Color(0xFF059669)         // Emerald Jade for Footnotes / Ta'liqah
val TypoHeading = Color(0xFFBE123C)          // Rubricated Carmine for Fasl & Bab headers
val TypoPoetry = Color(0xFF4F46E5)           // Indigo Royal for Bayt & poetic verses

// ==========================================
// 5. Accuracy & Confidence Tints
// ==========================================
val ConfHigh = Color(0xFF15803D)             // Sage Green (>90%)
val ConfMedium = Color(0xFFD97706)           // Ochre Amber (70-90%)
val ConfLow = Color(0xFFDC2626)              // Crimson Red (<70%)


